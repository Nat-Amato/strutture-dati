#include "matrice.h"

int main() {
  
  
  
	return 0;
}

matrice.h : contiene solo la def della classe
matrice.cpp: implementazione
main: test della matrice

nella parte private: la matrice è un tipoelem, un array di puntatori, inizializzato nel costruttore con due input. dopo l'allocazione
--> [...]
--> [...]
...
--> [...]

elementi[i] [j]
inizializzati gli elementi a 0
	
Volendo invece di definire un alias di double, possiamo già definire la classe di tipoelem
